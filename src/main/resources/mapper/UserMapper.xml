<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.twk.nccommunity.dao.UserMapper">
    <select id="selectById" resultType="User">
        select *
        from user
        where id = #{id};
    </select>

    <select id="selectByName" resultType="User">
        select *
        from user
        where username = #{username};
    </select>

    <select id="selectByEmail" resultType="User">
        select *
        from user
        where email = #{email};
    </select>

    <insert id="insertUser" parameterType="User" keyProperty="id">
        insert into user (username, password, salt, email, type, status, activation_Code, header_Url, create_time)
        values (#{username},#{password},#{salt},#{email},#{type},#{status},#{activationCode},#{headerUrl},#{createTime});
    </insert>

    <update id="updateStatus" parameterType="int">
        update user
        set status = #{status}
        where id = #{id};
    </update>

    <update id="updateHeader" parameterType="int">
        update user
        set header_url = #{headUrl}
        where id = #{id};
    </update>

    <update id="updatePassword" parameterType="int">
        update user
        set password = #{password}
        where id = #{id};
    </update>
</mapper>